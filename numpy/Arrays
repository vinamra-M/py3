import numpy

def arrays(arr):
    # complete this function
    # use numpy.array
    a = list(reversed(arr))
    b = numpy.array(a, float)
    return b

arr = input().strip().split(' ')
result = arrays(arr)
print(result)
